#include<stdio.h>

int main(){
    int a;
    int b;
    printf("Enter the numerator: ");
    scanf("%d",&a);
    printf("Enter the denominator: ");
    scanf("%d",&b);
    printf("The fraction is %d/%d", a, b);

    return 0;
}